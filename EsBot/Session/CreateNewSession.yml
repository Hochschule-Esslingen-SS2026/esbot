info:
  name: CreateNewSession
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}v1/sessions"
  body:
    type: json
    data: |-
      {
        "UserId" : "testID"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
