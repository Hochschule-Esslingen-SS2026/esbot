info:
  name: SessionGet
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}v1/sessions?userId=testID"
  params:
    - name: userId
      value: testID
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
